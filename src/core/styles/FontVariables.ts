export type FontTypes = {
  regular: string;
  medium: string;
  bold: string;
  light: string;
};
export const FontVariables: FontTypes = {
  regular: "OpenSansRegular, Arial, sans-serif",
  medium: "OpenSansMedium, Arial, sans-serif",
  bold: "OpenSansBold, Arial, sans-serif",
  light: "OpenSansLight, Arial, sans-serif",
};

export const FontSizes = {
  fontSizeBase: "14px",
};
